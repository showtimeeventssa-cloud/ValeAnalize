# ValeAnalize V22 — Artwork Rebuild

V22 keeps the V21 app functionality unchanged and rebuilds only the visual assets/cache references.

- Main artwork is scaled inside a larger safe canvas so the chrome pick has clear top/bottom breathing room.
- Header mark uses the complete pick/waveform artwork with safe margins.
- New iOS Apple Touch icons and manifest icons use V22 filenames to avoid stale icon paths.
- Service-worker cache is V22-specific.
- GP-50 analysis, guitar profile, preset generation, calibration and PRST workflow are unchanged from V21.
